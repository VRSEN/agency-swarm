---
title: "Installation"
description: "Install Agency Swarm in just 2 simple steps."
icon: "box"
---

## Installation
<Accordion title="Step 1: Create a Python Virtual Environment" defaultOpen={false}>

1. **Install Python**

   Download and install the latest version of Python from the [official Python website](https://www.python.org/downloads/).

2. **Create a Virtual Environment**

   Navigate to your project directory and create a virtual environment using `venv`:

   ```bash
   python -m venv venv
   ```

3. **Activate the Virtual Environment**

   On Mac or Linux:

   ```bash
   source venv/bin/activate
   ```

   On Windows:

   ```bash
   venv\Scripts\activate
   ```

</Accordion>
<Accordion title="Step 2: Install the agency swarm package"  defaultOpen={true}>

Simply run the following command:

<Note>Make sure you are in the virtual environment before installing the package.</Note>

```bash
pip install agency-swarm
```

</Accordion>

## Next Steps

When it comes to building your first AI Agency, you have three options:

<CardGroup cols={3}>
  <Card title="Starter Template" icon="layer-group" iconType="duotone" href="./getting-started/starter-template">
    Recommended for most users. Create your repository from the template and keep built-in AI rules.
  </Card>
  <Card title="AI Agents (Cursor)" icon="cube" iconType="duotone" href="./getting-started/cursor-ide">
    Customize the template quickly with Cursor while keeping framework-specific guidance.
  </Card>
  <Card title="From Scratch" icon="code" iconType="duotone" href="./getting-started/from-scratch">
    Build your own agents and tools from the ground up. Use this when you need full manual control.
  </Card>
</CardGroup>
